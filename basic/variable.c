#include <stdio.h>

int main() {
    int x;
    x = 2;

    int y = x*5; //declaration and expresion (x*5)

    printf("The magic number is :%i\nThe value of x is: %i\n", y, x); // need to convert int to string with %i
    return 0;
}